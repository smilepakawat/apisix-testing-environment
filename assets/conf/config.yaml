#
# APISIX standalone mode configuration
# config_provider: yaml  →  routes/upstreams are read from apisix.yaml (no etcd needed)
#

deployment:
  role: data_plane
  role_data_plane:
    config_provider: yaml

apisix:
  node_listen: 9080
  enable_admin: false

nginx_config:
  error_log_level: warn
